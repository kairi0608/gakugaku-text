"use client";import {useState} from "react";export function BackupPanel(){const[msg,setMsg]=useState("");async function backup(){const r=await fetch("/api/backup/export",{method:"POST",headers:{"content-type":"application/json"},body:"{}"});if(!r.ok){setMsg("バックアップを作成できませんでした");return}const blob=await r.blob(),a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download=`gakugaku-backup-${new Date().toISOString().slice(0,10)}.zip`;a.click();URL.revokeObjectURL(a.href);setMsg("バックアップを保存しました")}return <section className="card"><h2>バックアップ</h2><p>SQLiteとローカル画像、manifest.jsonをZIPにまとめます。</p><button className="button" onClick={backup}>ZIPをエクスポート</button>{msg&&<p className="notice">{msg}</p>}<hr/><label className="field"><span>バックアップを確認（自動上書きしません）</span><input type="file" accept=".zip"/></label></section>}

