import {BackupPanel} from "./BackupPanel";export default function Page(){const vars=["OPENAI_API_KEY","OPENAI_TEXT_MODEL","OPENAI_IMAGE_MODEL"] as const;return <main className="shell"><header className="page-head"><div><span className="eyebrow">このPCの設定</span><h1>ローカル設定</h1></div></header><section className="card"><h2>AI環境変数</h2><p>値そのものは安全のため表示しません。.env.local に設定して再起動してください。</p><div className="list">{vars.map(v=><div className="list-row" key={v}><span className="subject-dot"/><strong>{v}</strong><span className="pill">{process.env[v]?"設定済み":"未設定"}</span></div>)}</div></section><br/><BackupPanel/></main>}

