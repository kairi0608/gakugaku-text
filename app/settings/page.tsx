import { BackupPanel } from "./BackupPanel";
export default function Page() { const vars = ["NEXT_PUBLIC_SUPABASE_URL", "SUPABASE_SERVICE_ROLE_KEY", "OPENAI_API_KEY"] as const; return <main className="shell"><header className="page-head"><div><span className="eyebrow">クラウド設定</span><h1>設定</h1></div></header><section className="card"><h2>サーバー環境変数</h2><p>値そのものは安全のため表示しません。VercelのEnvironment Variablesへ設定してください。</p><div className="list">{vars.map(variable => <div className="list-row" key={variable}><span className="subject-dot"/><strong>{variable}</strong><span className="pill">{process.env[variable] ? "設定済み" : "未設定"}</span></div>)}</div></section><br/><BackupPanel/></main>; }
