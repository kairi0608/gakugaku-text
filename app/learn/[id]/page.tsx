import {notFound} from "next/navigation";import {getMaterial} from "@/lib/materials";import {LearnForm} from "./LearnForm";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params,m=getMaterial(id);if(!m)notFound();return <main className="shell"><header className="page-head"><div><span className="eyebrow">学習モード</span><h1>{m.title}</h1><p>全{m.document.questions.length}問。自分のペースで進めましょう。</p></div></header><LearnForm materialId={id} document={m.document}/></main>}
