import {notFound,redirect} from "next/navigation";import {getMaterial,saveMaterial} from "@/lib/materials";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params,m=getMaterial(id);if(!m)notFound();async function update(fd:FormData){"use server";const current=getMaterial(id);if(!current)return;const doc={...current.document,metadata:{...current.document.metadata,title:String(fd.get("title")),objective:String(fd.get("objective"))}};saveMaterial(doc,id);redirect(`/materials/${id}`)}return <main className="shell"><header className="page-head"><div><span className="eyebrow">新しいバージョンとして保存</span><h1>教材を編集</h1></div></header><form action={update} className="card"><label className="field"><span>タイトル</span><input name="title" defaultValue={m.title}/></label><label className="field"><span>学習目標</span><textarea name="objective" defaultValue={m.document.metadata.objective}/></label><button className="button">変更を保存</button></form></main>}

