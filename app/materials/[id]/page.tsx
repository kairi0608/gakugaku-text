import Link from "next/link";import {notFound} from "next/navigation";import {getMaterial} from "@/lib/materials";import {MaterialRenderer} from "@/components/materials/MaterialRenderer";import {MaterialPrintView} from "@/components/materials/MaterialPrintView";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params,m=getMaterial(id);if(!m)notFound();return <main className="shell"><header className="page-head no-print"><div><span className="eyebrow">バージョン {m.version.versionNumber}</span><h1>{m.title}</h1><p>{m.document.metadata.grade}・{m.document.metadata.subject}・{m.document.metadata.unit}</p></div><div className="actions"><Link className="button" href={`/learn/${m.id}`}>この教材で学ぶ</Link><Link className="button secondary" href={`/materials/${m.id}/edit`}>編集</Link><button className="button secondary" onClick={undefined}>印刷はブラウザメニューから</button></div></header><MaterialRenderer document={m.document}/><MaterialPrintView document={m.document}/></main>}

