import {NextResponse} from "next/server";import {z} from "zod";const schema=z.object({characterId:z.string().uuid(),stage:z.enum(["egg","child","learning-partner"])}).strict();export async function POST(req:Request){try{schema.parse(await req.json());if(!process.env.OPENAI_API_KEY)return NextResponse.json({error:"OPENAI_API_KEYが必要です。"},{status:503});return NextResponse.json({status:"accepted"})}catch{return NextResponse.json({error:"入力が不正です。"},{status:400})}}
