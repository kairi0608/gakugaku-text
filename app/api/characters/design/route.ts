import {NextResponse} from "next/server";import {z} from "zod";import {characterDesignSchema} from "@/features/materials/shared/schemas";import {db,characters} from "@/lib/materials";
const inputSchema=z.object({name:z.string().min(1),favoriteColor:z.string().min(1),personality:z.string().min(1),motif:z.string().min(1),likes:z.string().min(1),mood:z.string().min(1),request:z.string().max(1000)}).strict();
export async function POST(req:Request){try{const i=inputSchema.parse(await req.json()),now=new Date().toISOString(),id=crypto.randomUUID();const design=characterDesignSchema.parse({name:i.name,colors:i.favoriteColor.split(/[、,・]/).filter(Boolean),personality:i.personality,motif:i.motif,likes:i.likes,mood:i.mood,visualDescription:`${i.motif}をモチーフにした、${i.mood}学習パートナー`,imageBrief:`正方形。${i.favoriteColor}の配色。${i.motif}を殻の模様で示す、怖くないタマゴ1体。文字、数字、ロゴ、透かしなし。背景は簡素。`});db.insert(characters).values({id,name:i.name,stage:"egg",level:1,exp:0,designJson:JSON.stringify(design),createdAt:now,updatedAt:now}).run();return NextResponse.json({id,design,imageStatus:process.env.OPENAI_API_KEY?"ready-for-generation":"api-key-required"})}catch(e){console.error(e);return NextResponse.json({error:"キャラクターデザインを作成できませんでした。"},{status:400})}}

