import {NextResponse} from "next/server";import {z} from "zod";const schema=z.object({questionId:z.string(),answer:z.string().max(5000)}).strict();export async function POST(req:Request){try{schema.parse(await req.json());if(!process.env.OPENAI_API_KEY)return NextResponse.json({error:"記述式のAI評価にはOPENAI_API_KEYが必要です。"},{status:503});return NextResponse.json({status:"accepted"})}catch{return NextResponse.json({error:"評価入力が不正です。"},{status:400})}}
