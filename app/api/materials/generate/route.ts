import {NextResponse} from "next/server";import {generationInputSchema} from "@/features/materials/shared/schemas";import {sampleDocument,saveMaterial} from "@/lib/materials";
export async function POST(req:Request){try{const input=generationInputSchema.parse(await req.json());const doc=sampleDocument(input);const id=saveMaterial(doc);return NextResponse.json({id,imageStatus:process.env.OPENAI_API_KEY?"queued":"placeholder"})}catch(e){console.error("material generation failed",e);return NextResponse.json({error:"入力内容を確認して、もう一度お試しください。"},{status:400})}}
