"use client";import {useState} from "react";import {useRouter} from "next/navigation";export function CharacterForm(){const router=useRouter(),[error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const r=await fetch("/api/characters/design",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))}),j=await r.json();if(!r.ok)setError(j.error);else router.push("/characters")}return <form className="card form-grid" onSubmit={submit}>{[["name","名前","ミント"],["favoriteColor","好きな色","みどり・オレンジ"],["personality","性格","好奇心旺盛でやさしい"],["motif","モチーフ","森と星"],["likes","好きなもの","本と探検"],["mood","雰囲気","まるくて親しみやすい"]].map(x=><label className="field" key={x[0]}><span>{x[1]}</span><input name={x[0]} defaultValue={x[2]} required/></label>)}<label className="field full"><span>自由な要望</span><textarea name="request" defaultValue="怖くない、シンプルな相棒"/></label>{error&&<p className="notice error full">{error}</p>}<button className="button full">デザインとタマゴを作る</button></form>}

