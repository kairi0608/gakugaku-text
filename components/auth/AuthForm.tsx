import Link from "next/link";
export function AuthForm({ title, action, signup = false, forgot = false, reset = false }: { title: string; action: (data: FormData) => Promise<void>; signup?: boolean; forgot?: boolean; reset?: boolean }) { return <main className="auth"><Link href="/">← ガクガク教材Hub</Link><h1>{title}</h1><form action={action}>{signup && <label className="field">表示名<input required name="displayName" autoComplete="name"/></label>}{!reset && <label className="field">メールアドレス<input required type="email" name="email" autoComplete="email"/></label>}{!forgot && <label className="field">パスワード<input required minLength={8} type="password" name="password" autoComplete={reset ? "new-password" : signup ? "new-password" : "current-password"}/></label>}<button className="button" type="submit">{title}</button></form>{!forgot && !reset && <p className="muted">{signup ? <Link href="/auth/login">アカウントをお持ちの方</Link> : <><Link href="/auth/signup">新規登録</Link> ・ <Link href="/auth/forgot-password">パスワードを忘れた方</Link></>}</p>}</main> }
