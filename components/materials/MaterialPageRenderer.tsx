import type { MaterialDocument } from "@/features/materials/shared/types";import { MaterialBlockRenderer } from "./MaterialBlockRenderer";
export function MaterialPageRenderer({document,page}:{document:MaterialDocument;page:MaterialDocument["pages"][number]}){return <section className={`material-page ${document.presentation.pageSize} ${page.backgroundAssetId ? "has-material-background" : ""}`} style={{"--accent":document.presentation.colorPalette[0],"--warm":document.presentation.colorPalette[1],backgroundImage:page.backgroundAssetId?`linear-gradient(rgba(255,255,255,.88),rgba(255,255,255,.88)),url(/api/assets/${page.backgroundAssetId})`:undefined} as React.CSSProperties}>{page.blocks.map(b=><MaterialBlockRenderer key={b.id} block={b} document={document}/>)}</section>}
