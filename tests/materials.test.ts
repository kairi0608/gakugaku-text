import {describe,expect,it} from "vitest";import {materialDocumentSchema} from "@/features/materials/shared/schemas";import {claimExpiresAt,hashClaimToken,isClaimExpired,tokenMatches} from "@/lib/security/tokens";import {canManageClassrooms,roleHome} from "@/features/profiles/roles";const valid={version:1,metadata:{title:"分数",grade:"小5",subject:"算数",unit:"分数",objective:"理解する",difficulty:"standard"},presentation:{mode:"full-image",format:"worksheet-poster",pageSize:"a4-portrait"},pages:[{id:"p1",pageNumber:1,altText:"分数の問題"}],questions:[{id:"q1",pageId:"p1",order:0,prompt:"1/2+1/2",answerType:"number",correctAnswer:"1",explanation:"分母が同じ"}],feedbackPolicy:{tone:"gentle",revealAnswer:true,allowHints:true},accessibility:{highContrast:true,largeText:false,readingSupport:false,imageDescription:"分数問題"}} as const;describe("MaterialDocument",()=>{it("validates",()=>expect(materialDocumentSchema.safeParse(valid).success).toBe(true));it("rejects extras",()=>expect(materialDocumentSchema.safeParse({...valid,script:"bad"}).success).toBe(false));it("rejects area overflow",()=>expect(materialDocumentSchema.safeParse({...valid,questions:[{...valid.questions[0],answerArea:{x:.8,y:0,width:.3,height:.2}}]}).success).toBe(false))});describe("claim",()=>{it("hashes and compares",()=>{const h=hashClaimToken("token","secret");expect(h).not.toContain("token");expect(tokenMatches("token",h,"secret")).toBe(true)});it("expires after 24h",()=>{const now=new Date("2026-01-01T00:00:00Z");const expiry=claimExpiresAt(now);expect(expiry.toISOString()).toBe("2026-01-02T00:00:00.000Z");expect(isClaimExpired(expiry,new Date("2026-01-02T00:00:00Z"))).toBe(true)})});describe("roles",()=>{it("routes and authorizes",()=>{expect(roleHome("teacher")).toBe("/teacher");expect(canManageClassrooms("teacher")).toBe(true);expect(canManageClassrooms("personal")).toBe(false)})});
