import {describe,expect,it} from "vitest";import {materialDocumentSchema,characterDesignSchema,evaluationSchema} from "../features/materials/shared/schemas";import {autoGrade} from "../lib/grading";
const document={version:1,metadata:{title:"わり算",grade:"小学3年",subject:"算数",unit:"わり算",objective:"基本を理解",difficulty:"standard"},presentation:{format:"adventure",pageSize:"a4-portrait",visualTheme:"森",colorPalette:["#123456","#abcdef"]},pages:[{id:"p1",pageNumber:1,blocks:[{id:"t1",type:"title",text:"わり算"}]}],questions:[{id:"q1",pageId:"p1",order:1,prompt:"6÷2",answerType:"number",correctAnswer:"3",explanation:"6を2つに分ける"}],feedbackPolicy:{tone:"gentle",revealAnswer:false,allowHints:true},accessibility:{highContrast:true,largeText:true,readingSupport:true,imageDescription:"森の背景"}};
describe("MaterialDocument",()=>{it("strictly validates generated documents",()=>expect(materialDocumentSchema.safeParse(document).success).toBe(true));it("rejects unknown fields",()=>expect(materialDocumentSchema.safeParse({...document,script:"bad"}).success).toBe(false));it("rejects invalid JSON shape",()=>expect(materialDocumentSchema.safeParse({version:1}).success).toBe(false))});
describe("AI schemas",()=>{it("validates character design",()=>expect(characterDesignSchema.safeParse({name:"ミント",colors:["緑"],personality:"やさしい",motif:"森",likes:"本",mood:"明るい",visualDescription:"丸い相棒",imageBrief:"文字なしのタマゴ"}).success).toBe(true));it("rejects invalid evaluation",()=>expect(evaluationSchema.safeParse({verdict:"maybe",score:120}).success).toBe(false))});
describe("grading",()=>{it("grades normalized numbers",()=>expect(autoGrade("number"," ３ ","3")).toBe(true));it("grades multiple choice independent of order",()=>expect(autoGrade("multiple-choice","b,a","a,b")).toBe(true));it("rejects wrong answers",()=>expect(autoGrade("text","ねこ","いぬ")).toBe(false))});
